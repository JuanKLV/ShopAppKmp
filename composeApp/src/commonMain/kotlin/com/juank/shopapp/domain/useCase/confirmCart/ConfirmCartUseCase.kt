package com.juank.shopapp.domain.useCase.confirmCart

import com.juank.shopapp.domain.repository.confirmCart.ConfirmCartRepository

class ConfirmCartUseCase(private val confirmCartRepository: ConfirmCartRepository) {
}